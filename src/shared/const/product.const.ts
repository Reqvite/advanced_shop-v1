const minRating = 0;
const maxRating = 5;
const minPrice = 1;
const maxPrice = 500;
const defaultSort = 0;
const defaultPrice = [minPrice, maxPrice];
const defaultRating = [minRating, maxRating];
const skeletonLength = 5;
const defaultPage = 1;

export {
  defaultPage,
  defaultPrice,
  defaultRating,
  defaultSort,
  maxPrice,
  maxRating,
  minPrice,
  minRating,
  skeletonLength
};
