import {ReactElement} from 'react';
import {MessageBox, PageWrapper} from '@/shared/ui';

const SuccessPage = (): ReactElement => {
  return (
    <PageWrapper>
      <MessageBox />
    </PageWrapper>
  );
};

export default SuccessPage;
