import {ReactElement} from 'react';
import {PageWrapper} from '@/shared/ui';

const PrivacyPolicyPage = (): ReactElement => {
  return <PageWrapper>PrivacyPolicyPage</PageWrapper>;
};

export default PrivacyPolicyPage;
