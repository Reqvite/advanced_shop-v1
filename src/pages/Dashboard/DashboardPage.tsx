import {ReactElement} from 'react';
import {PageWrapper} from '@/shared/ui';

const DashboardPage = (): ReactElement => {
  return <PageWrapper>{/* <Bar /> */}</PageWrapper>;
};

export default DashboardPage;
